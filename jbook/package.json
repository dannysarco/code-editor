{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^9.0.7"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "test": "npm run test --workspaces --if-present",
    "smoke": "scripts/pack-smoke.sh"
  }
}
