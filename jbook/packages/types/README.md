# @my-scrapbook/types

Shared TypeScript types for [My Scrapbook](https://www.npmjs.com/package/my-scrapbook): the `Cell` shape, the `/cells` API wire contract, and the bundler result type.

Internal to the `my-scrapbook` packages. Source: [dannysarco/code-editor](https://github.com/dannysarco/code-editor).
